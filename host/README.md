# TUI skill evaluation workspace

This directory contains retained evaluation artifacts for
`tui-plugin-development`. It is not an npm package and is never mounted as a
separate Cordis plugin bundle.

The shipped skill and Host overlay live inside the single public
`@openma/deepseek-harness-tui` package under `npm/skills/` and `npm/lib/`.
